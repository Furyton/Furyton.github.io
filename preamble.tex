\usepackage{marvosym} % Allows the use of symbols
\usepackage{parskip} % Spacing adjustments

% Main font

\usepackage{fontspec}
% \setmainfont{Raleway}[
%   Path           = ./fonts/,  % Adjust the path to match where you've placed the fonts
%   Extension      = .ttf,      % or .otf, depending on the font files you have
%   UprightFont    = *-Regular,
%   BoldFont       = *-Bold,
%   ItalicFont     = *-Italic,
%   BoldItalicFont = *-BoldItalic
% ]
% \setsansfont{Raleway}[
%   Path           = ./fonts/,  % Adjust the path to match where you've placed the fonts
%   Extension      = .ttf,      % or .otf, depending on the font files you have
%   UprightFont    = *-Regular,
%   BoldFont       = *-Bold,
%   ItalicFont     = *-Italic,
%   BoldItalicFont = *-BoldItalic
% ]

% % Chinese font

% \usepackage{xeCJK} % For Chinese text
% \setCJKmainfont[Path = ./fonts/]{kaiti.ttf}

% Header and footer

\usepackage{fancyhdr}

\pagestyle{fancy}
\fancyhf{}
\fancyfoot[L]{\textcolor{gray}{\footnotesize\emph{Updated \today}}}
% \fancyfoot[R]{\textcolor{gray}{\footnotesize\emph{Available at \href{https://github.com/jhelvy/cv}{https://github.com/jhelvy/cv}}}}
\fancyfoot[R]{\textcolor{gray}{\footnotesize\emph{Page \thepage\ of \pageref{LastPage}}}}
\renewcommand{\headrulewidth}{0pt} % Removes the header line
\renewcommand{\footrulewidth}{0pt} % Removes the footer line
\usepackage{lastpage} % Required for \pageref{LastPage}

% Links

\usepackage{hyperref}
\usepackage{color}
\definecolor{linkcolour}{rgb}{0,0.2,0.6}
\hypersetup{colorlinks,breaklinks,urlcolor=linkcolour, linkcolor=linkcolour}
